# Simple Hello Ostrich plugin

This template provides a minimal reference implementation for Ostrich SDK Osplates. It demonstrates how parameter rendering, environment variable resolution.

## Configuration

Display a sample config file with the command:

```bash
ost template config hello
```
