### Display Docker Task
This task runs inside a Docker container using the `container` runner.
It illustrates how to execute commands across different container images and access environment variables.
