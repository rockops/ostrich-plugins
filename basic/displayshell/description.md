### Display Shell Task
This task executes commands directly on the host machine using the `shell` runner.
It demonstrates how to run local system commands and access both template parameters and environment variables.
