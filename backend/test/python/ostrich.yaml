# ====== python backend configuration =======
plugin:
  # Name of the plugin
  name: my-python-backend
  # Version of the plugin
  version: 0.0.1
  # Short description
  business_name: My Python Backend Application

template:
  # Kind of template to use
  kind: backend
  
  params:
    port_external: 8081
    port: 8000
    input:
      # relative path to the source folder
      src: src
    # Name of the resulting docker image.
    image_name: my-python-backend
    registry: harbor.kubelb.home/ostrich/docker
    
    kubernetes:
      exposure:
        hostname: my-python-backend.kubelb.home

    gateway:
      create: false
      name: envoy
      namespace: envoy-gateway
